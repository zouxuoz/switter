{
  "name": "switter-client",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "moment": "^2.20.1",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-redux": "^5.0.6",
    "react-remarkable": "^1.1.3",
    "react-scripts": "1.1.0",
    "redux": "^3.7.2",
    "switter-actions": "^1.0.0"
  },
  "scripts": {
    "start": "eval $(cat ../../.env | sed 's/^/export /') && react-scripts start",
    "build": "eval $(cat ../../.env | sed 's/^/export /') && react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
